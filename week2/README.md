HW3 - EOF까지 정수 세 개를 입력받아 크기 순으로 출력, 배열 X

1. 최대 최소 중간값 찾는법


HW4 - 프로그램 매개변수로 받아 계산하는 계산기, if로 예외처리

1. System.exit(0); -> 바로 종료
2. ASCII Code로 정수인지 아닌지 확인
3. Integer.parseInt() -> String을 Int로 변경
4. .charAt() -> String으로 지정된 문자열에서 한 글자만 선택해서 char형태로 변환, Scanner로 받으면 String으로 받음
5. switch case 사용